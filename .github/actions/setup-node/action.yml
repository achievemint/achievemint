name: "Setup node"
description: "reference .nvmrc to configure node"
runs:
  using: "composite"
  steps:
    - name: Use Node.js
      uses: actions/setup-node@v6
      with:
        node-version-file: '.nvmrc'
        cache: 'npm'
    - name: Install dependencies
      shell: bash
      run: npm ci
